data/raw/
data/interim/
*.ckpt
wandb/
__pycache__/
.venv/
A1_form.md
A1_form.mdZone.Identifier

# Local convenience only (never a graded/submitted file): a Kaggle-runnable notebook for the
# full-corpus GPU build, generated by re-running this repo's own scripts/build_index.sh logic
# with configs/config.yaml's dev.max_pages set to 0. Kept at repo root, outside notebooks/, and
# gitignored so it never becomes part of the tracked structure per the handbook's "don't add
# extra files" rule -- it's just something to upload to Kaggle, not a repo deliverable.
/kaggle_build_index.ipynb
